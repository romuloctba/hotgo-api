import { CustomerIdValidatorPipe } from './customer-id-validator.pipe';

describe('CustomerIdValidatorPipe', () => {
  it('should be defined', () => {
    expect(new CustomerIdValidatorPipe()).toBeDefined();
  });
});
