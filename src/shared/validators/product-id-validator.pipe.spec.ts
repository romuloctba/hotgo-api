import { ProductIdValidatorPipe } from './product-id-validator.pipe';

describe('ProductIdValidatorPipe', () => {
  it('should be defined', () => {
    expect(new ProductIdValidatorPipe()).toBeDefined();
  });
});
