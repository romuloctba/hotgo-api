import { UserIdValidatorPipe } from './user-id-validator.pipe';

describe('UserIdValidatorPipe', () => {
  it('should be defined', () => {
    expect(new UserIdValidatorPipe()).toBeDefined();
  });
});
