import { SupplierIdValidatorPipe } from './supplier-id-validator.pipe';

describe('SupplierIdValidatorPipe', () => {
  it('should be defined', () => {
    expect(new SupplierIdValidatorPipe()).toBeDefined();
  });
});
