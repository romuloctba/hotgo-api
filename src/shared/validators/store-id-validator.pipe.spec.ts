import { StoreIdValidatorPipe } from './store-id-validator.pipe';

describe('StoreIdValidatorPipe', () => {
  it('should be defined', () => {
    expect(new StoreIdValidatorPipe()).toBeDefined();
  });
});
